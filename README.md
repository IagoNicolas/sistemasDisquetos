# RSA with OpenSSL
## Compile with:

gcc -o sistemasDisquetos main.c

## Run with:

./sistemasDisquetos

## Compile test with:

gcc -o test_main test_main.c -lssl -lcrypto -lcunit

## Run test with:

./test_main